terraform {
  backend "s3" {}  # The actual configuration will be provided via CLI

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 3.0"
    }
  }
}

provider "aws" {
  region = var.aws_region
}